1. 当前项目是一个前后端分离的项目，后端目录./backend/，前端目录./frontend/
2. 后端技术架构为FastAPI，前端技术架构为React+Vite
3. 此系统全称为：“基于机器学习的校园网钓鱼邮件智能过滤系统的设计与实现”
4. 后端启动方式为uv run --env-file .env uvicorn app.main:app
5. 前端启动方式为npm run dev
6. 给后端代码添加详细的谷歌风格中文注释，无需作者、邮箱、版本号。
7. 后端每个代码文件不要超过500行，超过则将可以复用的代码封装为工具类重复使用。
8. 对于可以复用的工具类，要主动封装为工具类来调用，避免整个项目代码冗长。
9. 后端代码要符合FastAPI的CRUD标准。

[用中文回答我]进入计划模式开始完成我的需求。主要做的东西是一个“基于机器学习的校园网钓鱼邮件智能过滤系统的设计与实现”系统，用户端主要功能为：1.使用学号和密码登录系统；2.进入系统后首页是一个可以收发邮件的网页邮件客户端；3.邮件账号默认为自己的'学号@hhstu.edu.cn'学生邮箱；4.用户可以自行添加QQ邮箱、163邮箱，默认只填入自己的邮箱号和密码即可，默认采用imap协议，并且对于QQ邮箱和网易163邮箱提供默认配置，无需填写配置，但是用户可以自行填写imap配置来添加自己的其他品牌的邮箱；5.当用户添加多个邮箱后，首页的收发邮件客户端即可聚合显示多个邮箱的邮件，或者单独显示某个邮箱的邮件，当首次链接成功后，自动拉取该邮箱的最近20封邮件；6.对于被检测扫描为钓鱼邮件的邮件，则高亮显示，显示分为两种：正常、高危；7.当高危钓鱼邮件时，将链接隐藏，并且在链接位置生成一个按钮：“点击按钮查看疑似钓鱼链接”，点击后弹出链接的完整url，并且要求用户输入自己的学号从而允许复制，否则禁用用户的复制功能和文本选择功能。       实现要求如下：1.钓鱼邮件的核心机器学习逻辑暂不实现，但是要给我预留接口，以便后续我训练好后自行与系统对接；2.接下来给我实现后续我的需求页面。                 所有代码要采用面向对象的编程思想，并且添加详细的谷歌风格中文注释，无需作者、邮箱、版本号。注意单个文件代码不要过长，尽量保持不超过500行代码。在后端中，对于可复用的工具类，要合理的将工具类进行拆分，封装为一个工具类，以便后续代码可以重复规范调用。        可以忽略现在的ui风格，微调ui，设计为更加现代化、极简、有个性的ui风格，保持企业化风格，类似于教务系统，添加丰富的动画效果，但要保持代码规范不混乱。可以自行使用我的数据库，我的数据库是mysql，部署在本地的docker中，用户名Hierarch，密码869125，你可以根据我的需求来创建对应的数据库并构建合理的字段。代码编写完成后自动运行测试，确保代码没问题再给我交付。实际效果要具备高并发、数据库存储要合理，用户首次登录时在数据库中存储邮件，后续登陆时则只自动拉取最新邮件，而不拉取往期邮件。当前实现了部分功能但效果不理想，可以给我重构代码，代码质量要参考Gmail网页版的质量，确保多个用户进行拉取邮件，收发邮件不卡顿，数据库负担不大。